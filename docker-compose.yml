version: '3'
services: 
    api:
        build: 
            context: ./api/
            dockerfile: Dockerfile
        ports:
            - 3000:3000
        volumes:
            - ./api:/api
        stdin_open: true
        tty: true